/**
 * SDK re-exports of shared protocol vocabulary from @rezprotocol/core.
 */
export { CONTRACT_VERSION, REZ_CONTRACT_TYPES } from "@rezprotocol/core";
export {
  MESH_ADDRESS_KINDS,
  buildInboxAddress,
  buildRendezvousAddress,
  isMeshAddress,
  assertValidMeshAddress,
} from "@rezprotocol/core";
